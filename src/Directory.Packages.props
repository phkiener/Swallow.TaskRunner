<Project>
  <!-- Do not inherit the parent Directory.Packages.props so the changes to package dependencies are explicitly controlled. -->
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
  </PropertyGroup>
  <ItemGroup>
  </ItemGroup>
</Project>
